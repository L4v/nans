function sorted = sortByFirstColumn(I)


[r, ~] = size(I);

for i = 1:r-1
   for j = 1:r-i-1
       X = I(j, :);
       Y = I(j + 1, :);
       if X(1, 1) < Y(1, 1)
          Tmp = X;
          X = Y;
          Y = Tmp;
       end
       
   end
end

end